services:
  task-manager-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-manager-api
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
    restart: unless-stopped
